"""自定义文案模式 prompts。"""

CONTENT_CUSTOM_USER = """你正在协助用户反复打磨一份短视频台词脚本。请严格输出 JSON，不要输出 JSON 以外任何内容。

用户本轮需求：
{user_message}

可用素材（可能为空）：
{input_text}

当前草稿（角色+台词，可能为空）：
{current_draft}

历史对话摘要（可能为空）：
{chat_summary}

用户历史发言（可能为空）：
{user_history}

任务要求：
1. 结合用户本轮需求、已有草稿和历史约束，产出“可直接用于配音”的台词脚本。
2. 自动识别并抽取主要有台词的角色；角色命名清晰、稳定，避免同义重复角色，不要为仅被提及或一闪而过的人物单独建角色。
3. 若当前草稿里同一主体存在不同形态/阶段/身份版本（例如“龙形态/人形态”“幼年/成年”“伪装/真身”），必须保留为不同角色，不能为了简化而合并成一个泛称角色；除非用户明确要求合并。
4. 当不同形态被保留时，角色名必须能明确区分形态，并在 dialogue_lines 中持续使用对应形态名，不要前面叫“龙王（龙形态）”、后面又混成“龙王”。
5. 输出台词总字数（不含标点）约 {total_chars} 字，可在合理范围内浮动。
6. 每条台词都要有明确说话人和自然中文标点，语气应贴近角色设定。
7. 若用户明确提出修改方向，应在现有草稿基础上迭代，而不是完全忽略历史约束。
8. 若素材信息不足，不要编造可核查事实；可采用中性表达、通用场景或明确不确定性。
9. 标题简洁、有传播感，适合短视频展示。
10. 若同一角色是连续发言，必须合并为一条 dialogue_lines 记录，不要拆成多条相邻同角色台词。
11. 不要输出 JSON 以外任何内容

输出 JSON 结构（字段名必须一致）：
{{
  "title": "标题",
  "roles": [
    {{
      "name": "角色名",
      "description": "角色定位/语气/功能"
    }}
  ],
  "dialogue_lines": [
    {{
      "speaker_name": "角色名",
      "text": "台词内容"
    }}
  ]
}}"""
