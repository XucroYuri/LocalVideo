"""双人播客文案类型专属 prompts。"""

DUO_PODCAST_STYLE_DESCRIPTIONS = {
    "无预设": "不预设固定风格，按素材语义自然展开双人对话，保持信息密度、节奏和角色互动的平衡",
    "默认": "不预设固定风格，按素材语义自然展开双人对话，保持信息密度、节奏和角色互动的平衡",
    "双讲拆解型": "一人先给结论并结构化拆解逻辑与证据，另一人持续追问关键细节并做生活化翻译，强调解释-追问-回应-推进的节奏",
    "辩论对抗型": "立场对立、互相攻防，强调证据、反驳、让步与总结，结尾给出裁决、共识或分歧点",
    "评论解读型": "一人抛出素材或事件，另一人拆解背景与影响，突出脉络、因果、立场与风险",
    "“主持人 + 专家”拆解型": "主持人代表大众提问，专家负责结构化输出，突出问题树、概念澄清与可执行建议",
    "“主讲 + 捧哏”喜剧型": "一人负责输出段子与观点，另一人负责接梗、抬杠与递台阶，突出包袱、回马枪、重复梗与人设反差",
    "互怼吐槽型（损友感）": "高频打断、互相拆台，用夸张表达制造笑点或爽点，突出挑刺、反讽、抓语病与轻量翻旧账",
    "访谈挖掘型（采访感）": "围绕明确目标层层追问，不闲聊，强调细节、转折与关键选择点",
    "复盘推理型": "两人协同拼接信息，从线索到结论，强调假设、验证、证据链与排除法",
    "“共情陪伴”疗愈型": "两人分享经历、互相接住情绪，给温柔但不说教的建议，强调你不孤单、具体感受、边界与可尝试的小步",
}

CONTENT_DIALOGUE_DUO_USER = """请根据以下事件信息，创作一段双人播客对话文案，并严格输出 JSON。

事件信息：
{input_text}

已知角色设定：
- 讲述者1：{role_1_name}（{role_1_description}）
- 讲述者2：{role_2_name}（{role_2_description}）
- 场景：{scene_name}（{scene_description}）

要求：
1. 总字数（不含标点）约 {total_chars} 字，每人单次发言不少于 20 字
2. 对话风格：{style_desc}
3. 在保持风格要求的前提下，整体节奏贴近知识播客：一位负责结构化讲解，另一位负责追问、澄清、补充与总结，形成“解释-追问-回应-推进”的推进感
4. 开场必须自然：前2~4句先完成“问候/引题/界定今天要聊的问题”，不要第一句直接进入结论
5. 强化“对话感”：允许自然口语衔接（如“等等、我追问一下、你的意思是”），避免把独白硬拆成两人轮流朗读
6. 只能有 2 个对话角色（讲述者1与讲述者2），场景信息仅用于语境，不可作为说话人
7. 角色设定是固定约束，不可改写人物名称、角色定位与场景设定
8. 每句台词必须有标点，便于后续切分与配音
9. 信息密度高，尽量用具体事实、因果和例子推进，不要空话和套话
10. 若同一讲述者连续发言，必须合并为一条 dialogue_lines 记录，不要拆成多条相邻同 speaker 的台词
11. 不要输出 JSON 以外任何内容

输出 JSON 结构：
{{
  "title": "标题",
  "dialogue_lines": [
    {{"speaker_id": "ref_01", "speaker_name": "{role_1_name}", "text": "台词内容"}},
    {{"speaker_id": "ref_02", "speaker_name": "{role_2_name}", "text": "台词内容"}}
  ]
}}"""


CONTENT_CUSTOM_DUO_USER = """你正在协助用户反复打磨一份双人播客对话文案。请严格输出 JSON，不要输出 JSON 以外任何内容。

用户本轮需求：
{user_message}

可用素材（可能为空）：
{input_text}

当前草稿（角色+台词，可能为空）：
{current_draft}

历史对话摘要（可能为空）：
{chat_summary}

用户历史发言（可能为空）：
{user_history}

固定角色设定（不可改写）：
- 讲述者1：{role_1_name}（ID: {role_1_id}；描述：{role_1_description}）
- 讲述者2：{role_2_name}（ID: {role_2_id}；描述：{role_2_description}）
- 场景：{scene_name}（{scene_description}）

任务要求：
1. 结合用户本轮需求、已有草稿和历史约束，产出“可直接用于配音”的双人播客对话。
2. 对话只允许讲述者1与讲述者2发言；场景信息仅用于语境，不可作为说话人。
3. 输出总字数（不含标点）约 {total_chars} 字，可在合理范围内浮动；每人单次发言不少于 20 字。
4. 强化“对话感”与“解释-追问-回应-推进”的节奏，避免把独白硬拆成两人轮读。
5. 若用户明确提出修改方向，应在现有草稿基础上迭代，而不是完全忽略历史约束。
6. 若素材信息不足，不要编造可核查事实；可采用中性表达、通用场景或明确不确定性。
7. 每句台词必须有自然中文标点；同一讲述者连续发言必须合并为一条 dialogue_lines 记录。
8. 标题简洁、有传播感，适合短视频展示。
9. 不要输出 JSON 以外任何内容。

输出 JSON 结构（字段名必须一致）：
{{
  "title": "标题",
  "dialogue_lines": [
    {{
      "speaker_id": "{role_1_id}",
      "speaker_name": "{role_1_name}",
      "text": "台词内容"
    }},
    {{
      "speaker_id": "{role_2_id}",
      "speaker_name": "{role_2_name}",
      "text": "台词内容"
    }}
  ]
}}"""


"""双人播客视频描述专属 prompts。"""

VIDEO_PROMPT_DUO_PODCAST_SINGLE_TAKE_USER = """请为以下双人播客口播分镜生成{target_language_name}视频描述。

生成范围：
{generation_scope}

双人播客角色设定：
- 讲述者1：{role_1_name}（{role_1_description}，座位：{role_1_seat_label}）
- 讲述者2：{role_2_name}（{role_2_description}，座位：{role_2_seat_label}）
- 场景：{scene_name}（{scene_description}）
- 发言分布：{speaker_distribution}

注意：你只需要输出“动作层描述”。固定场景、固定镜头、口型约束、座位方向、禁字幕等规则会由系统自动拼接，不需要你重复输出。

视频生成参数（请据此控制画面设计）：
{video_spec_summary}

分镜内容：
{shots_display}

可用参考信息（按需选用，不可编造ID）：
{reference_info}

硬性要求（必须全部满足）：
1. 每条 video_prompt 仅描述该分镜的“动作层内容”：两位讲述者的表情、手势、姿态、互动细节。
2. 不要输出固定规则文本（如固定机位、固定场景、谁开口谁闭嘴、禁字幕、座位方向等），这些将由系统自动拼接。
3. 动作必须与该段口播语义严格对应，避免空泛词和套话。
4. 当前发言者动作可稍丰富，另一位仅保留安静聆听的细微动作（如轻点头、目光跟随、克制微笑）。
5. 严禁出现跨分镜依赖表达：例如“无缝转场、承接上一镜、接上个镜头、然后切到”等。
6. 角色称呼统一使用“左侧角色/右侧角色”，不要使用真实人名，也不要改用男/女讲述者等其他叫法。
7. {video_prompt_length_requirement}
8. 每个分镜都必须返回 video_reference_slots（数组），用于明确顺序与名称；若该分镜不需要参考图，返回空数组 []。
9. video_reference_slots 元素格式固定为 {{"order": 1, "id": "ref_01", "name": "参考名称"}}；id 只能从“可用参考信息”里选，禁止编造。

请以JSON格式输出：
{{
  "video_prompts": [
    {{
      "shot_index": 0,
      "video_reference_slots": [{{"order": 1, "id": "ref_01", "name": "参考名称"}}],
      "video_prompt": "{video_prompt_example_1}"
    }},
    {{
      "shot_index": 1,
      "video_reference_slots": [],
      "video_prompt": "{video_prompt_example_2}"
    }}
  ]
}}"""


"""双人播客首帧图描述专属 prompts。"""

FIRST_FRAME_BATCH_DUO_SYSTEM = """你是一个专业的双人播客首帧设计师，擅长为固定同框、一镜到底语境下的双人播客视频设计首帧构图。

关键原则（必须严格遵守）：
1. 这是固定双人同框的首帧任务，两侧角色默认保持普通静坐，仅允许轻微表情、目光、头部、肩颈与身体重心的细微差异；避免明显手势、大幅动作或连续动作链。
2. 角色称呼统一使用“左侧角色/右侧角色”，不要使用真实人名，也不要改用男/女讲述者等其他叫法。"""

FIRST_FRAME_BATCH_DUO_USER = """请为以下双人播客分镜设计首帧图像的构图描述。

生成范围：
{generation_scope}

参考信息：
{references}

要求：
1. 每条 first_frame_prompt 使用{target_language_name}，且仅描述该分镜的“首帧瞬间变化层”：两位讲述者的表情、手势、姿态与目光关系。
2. 只输出“首帧瞬间变化层描述”，不要重复固定机位、固定场景、座位方向、稳定布光、禁文字等系统规则。
3. 首帧构图中两位角色应为普通静坐状态，仅允许轻微表情和极小幅度姿态差异；不要写明显手势或大幅动作。
4. 首帧图只基于分镜开场可见画面与参考信息生成；不要根据口播/台词推导复杂剧情。
5. 角色称呼统一使用“左侧角色/右侧角色”，避免真实人名与男/女讲述者称呼。
6. {first_frame_prompt_length_requirement}
7. 每个分镜都必须输出 first_frame_reference_slots（数组），不得省略；若该分镜不需要参考图则输出 []。
8. first_frame_reference_slots 必须按以下顺序输出（按 order 升序）：
   - 若参考信息中存在“身份：场景”的参考项，它必须在第1位；
   - 其后按座位顺序放角色参考：左侧角色在前，右侧角色在后；
   - 仅保留本分镜实际使用的ID，且去重。
9. first_frame_reference_slots 元素格式固定为 {{"order": 1, "id": "ref_01", "name": "参考名称"}}，禁止编造ID。

请以JSON格式输出：
{{
  "frames": [
    {{
      "shot_index": 0,
      "first_frame_reference_slots": [{{"order": 1, "id": "ref_01", "name": "参考名称"}}],
      "first_frame_prompt": "{first_frame_prompt_example}"
    }},
    ...
  ]
}}"""
